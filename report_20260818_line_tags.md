# LINE公式「おすすめのタグ」の傾向分析と、tagm への実装（2026-08-18）

このドキュメントだけで作業を再開できるように書いてある。

---

## 1. 目的

tagm のAIタグ付けを、LINE公式の「おすすめのタグ」に近い発想へ寄せる。あわせて、
LINEと同じく **候補を多めに出して、そこからユーザーが選べる** 形にする。

- 時短したい人 → AIが選んだ9個をそのまま使う
- こだわりたい人 → 候補から差し替える／自分で検索して9個にする

---

## 2. LINE公式の実測（画面録画5本・50スタンプ）

### 確定した仕様

| 項目 | 実測値 |
|---|---|
| おすすめタグの提示数 | **常に18個**（50件すべてで例外なし） |
| 設定できるタグ数 | 最大9個（`9/9` 表示） |
| 生成タイミング | 選択0/9でも18個出る → **画像1枚に対する一発推論**。ユーザー選択に依存しない |
| UIの定義文 | 「画像に合うタグを以下から選んでください。画像ごとに9個までタグを設定できます。」 |
| 語彙 | LINE公式の**閉じた固定タグ集合**から選択。自由生成ではない |

### 推薦ロジックは7枠構成

1. **直訳**（2〜3個）画像内の文字をそのままタグ化。**略語も展開する**
   （おは→おはよう / いてら→いってらっしゃい / たす→助かる / はらへ→空腹・ごはん / しょぼん→しょんぼり）
2. **言い換え**（4〜6個）同義・同場面の別の言葉
3. **相手が打つ言葉**（2〜3個）★LINE独自。受け手・催促する側が入力しそうな語
   （「今は返信できません」→ 既読・未読・まだ？・おーい）
4. **絵柄から**（2〜3個）写っているモノ・ポーズ・動作
   （時計→もうすぐ着く / カバン→出かける / マグカップ→休憩・飲む / 走る→急ぐ）
5. **感情・反応**（2〜3個）表情から
6. **定番**（2〜3個）汎用の高頻度語
7. **ノイズ**（0〜2個）★採用しない。下記参照

### ノイズ枠（真似しない）

- **季節タグ**：暦と無関係。**8月収録の動画に 春・冬・夏・年末年始 が全部出た**。
  当初「7月だから夏タグ」と推定したが誤り。ただの低関連の埋め草。
- **動物種の当てずっぽう**：デフォルメが強く種を特定できないと クマ/トリ/イヌ/ネコ/ウサギ を乱れ打ち。
  ただし**実写犬では `イヌ` を全件で正確に1個**出した。→ 「確信が持てるときだけ1個」が正しい扱い。

### 一致率（ユーザーが選んだ9個 ⊂ LINEの18個）

| 素材 | 一致率 |
|---|---|
| 実写犬・日常フレーズ（動画5） | **約76%**（「はらへ」は7/7で完全一致） |
| 線画ブロブ・専門用語混じり（動画4） | **約48%** |

**一致率 ≒ 文字が日常トーク語彙に近いか × 絵柄が明確か**

文字が公式語彙から遠い（公差外／偏心あり／張力不足）と、LINEは文字を捨てて**絵柄100%**に切り替わる。
表情の誤読が最大の事故要因（「異常なし」の笑顔＋開口を怒りと誤読 → 毒舌/キレる/馬鹿/こらー で一致1/9）。

### 方針決定

**LINEの出力をそのまま真似ない。観点と構成だけ合わせ、ノイズ枠は除外する。**
理由：タグは検索キーなので、LINEの誤読をコピーすると誤マッチが増える。

---

## 3. 実装（tagm-LINEtest.html）

`tagm-test.html` は別作業で使用中のため触らない。**LINE対応版は `tagm-LINEtest.html`。**
（tagm-test.html は 159,446 バイトの元の状態に復元済み・バイト一致確認済み）

### 変更点

| # | 箇所 | 内容 |
|---|---|---|
| 1 | `aiBuildTaggingInstruction` | 旧9観点の羅列を廃止。冒頭に「タグ＝トーク画面で打つ検索キーワード」の定義を置き、上記7枠（ノイズ除く）＋禁止事項に全面書き換え |
| 2 | 同上 | 旧文の「言い換え・翻訳からタグを作らないこと」を解除。創作タグ禁止は「リストに無い語は出力しない」で担保し、**言い換え・略語展開・意訳は明示的に許可** |
| 3 | `AI_CANDIDATE_MAX = 18` / `aiGetCandidateCount()` | 採用数の2倍（上限18）を生成させる |
| 4 | `aiRunTagging` | 上位 `perStickerCount` 個を `state.tags` に採用、全18個を `state.suggestions[norm]` に保持 |
| 5 | `state.suggestions` | 新フィールド。localStorage には入るが **JSONエクスポートには含まれない** |
| 6 | `renderStickers` | 各行の下に破線の「おすすめ」帯。採用済み・必須タグは自動で除外表示 |
| 7 | `applySuggestion()` | 帯のチップをクリックで採用。`addTag()` を通さないのは、AIが提案したオートサジェストタグも採用できるようにするため |
| 8 | `jsAttr()` | onclick 属性に値を埋めるための二重エスケープ（HTML属性 × JS文字列） |
| 9 | `resetAll()` | `aiRejectedTags` / `suggestions` を初期化（未初期化で removeTag が例外を出す既存バグも解消） |
| 10 | `aiRunTagging` | **DEBUG**: 検証で落ちたタグの内訳をコンソールに出力（`window.aiLastDropLog`） |

### UI

```
[大丈夫] [連絡] [問題ない] ... 9個          ← 採用済み（実線チップ）
──────────────────────────────
おすすめ (未読) (既読) (返信不要) ... 9個    ← 差し替え候補（破線チップ）
```
合計18個が画面に出る。LINEは18個フラット＋採用済みハイライトだが、**A案（採用/候補を分ける）を採用**。

### 未対応

**穴埋めモード（`aiRunFillIn` / `aiBuildFillInInstruction`）は旧9観点のまま。候補も保存しない。**
全スタンプが9/9で埋まっていると穴埋めは「不足しているコマがありません」で終了するため、
**必ず「全部上書き」で実行すること**。

---

## 4. 検証状況

### 済み

- jsdom による自動検証 **30項目 全PASS**（`scratchpad/verify.js`。jsdom を npm install して `node verify.js`）
  - プロンプト内容、おすすめ帯のレンダリング、DOM構造、クリック採用、エスケープ、JSONエクスポート汚染なし、resetAll
- フィルタ書き換えの等価性：ランダム20万ケースで旧ロジックと**不一致0件**
- 実APIでの動作確認（Gemini `gemini-flash-latest`）済み

### 実測結果（各16枚）

| 指標 | A: 装飾スタンプ（文字なし） | B: 会話スタンプ（文字入り） |
|---|---|---|
| 後半（候補9個）どうしの一致率 | **50%** | **13%** |
| 固有タグ（その1枚にしかない語） | 1.8個/枚 | **5.6個/枚** |
| 固有タグゼロの枚数 | 3枚 | **0枚** |
| 全枚数に出るタグ | **5個**（かわいい/きらきら/キュート/素敵/ラブリー） | **0個** |

**惰性はAIの問題ではなく素材の問題。`thinkingBudget` を上げる必要はない。**
Bセットでは「相手が打つ言葉」枠が機能している（ただいま→おかえり・待ってた・会いたい）。

### 18個に届かない件（原因確定済み）

```
AI応答が目標未満だったのは 0枚
合計: AI応答 288個（= 16枚 × 18個ちょうど）→ 採用可 255個
リスト外で除外 33 / 重複 0
```

**AIは必ず18個出している。11%が「公式リストに無い語」として捨てられている。**
公式タグは610語。捨てられた29種を正規化照合したが**表記ゆれは0件**、全て創作。

| タイプ | 例 |
|---|---|
| 1. 公式の言い方を知らない | 笑顔(→にっこり) / お疲れ様(→おつかれさま) / 大好き(→好き) / 連絡する(→連絡) / 間違い(→間違いない) |
| 2. 物体名（語彙自体が無い） | ビーチ / 浮き輪 / ペットボトル / ベッド / 布団 / 夜 / スイーツ / 散歩 |
| 3. 言い換えの作りすぎ | 15番「またね」で7個溢れ、候補が11個に半減（またあとで/後で/待ってて/後ほど…） |

---

## 5. 未決の論点（ここから再開）

### 論点1: リスト外の脱落を減らすか

- **案A**: プロンプトに「よくある間違い」の対応表を入れる（笑顔→にっこり 等）。小さく確実。
- **案B**: 「4. 絵柄から」の枠を「モノの名前ではなく、そのモノから連想して**ユーザーが打つ言葉**」に書き換える。
  現状の書き方が物体名を作らせる誘導になっている（設計ミス）。
- **案C**: リスト外が出たら再問い合わせして補充。確実だがAPIコール増。**非推奨。**

推奨は **A+B**。ただし対応表は対症療法で、別セットでは別の語が出る。
根本は「610語をモデルが暗記しきれていない」ことなので完全には潰せない（確信度: 中）。

**実害は「候補が18個より少ない」だけ。採用される9個は全枚数で埋まっており、品質は落ちていない。**

### 論点2: 穴埋めモードをどうするか

- 案1: 穴埋めでも18個生成し、不足分だけ採用・残りを候補に（一貫する。推奨）
- 案2: 今のまま（旧プロンプト・候補なし）と割り切る
- 案3: 穴埋めを廃止し全部上書きに一本化

### 論点3: tagm.html への移植

tagm-LINEtest.html で固まったら本体へ。未着手。

---

## 6. 再開手順

```bash
# ローカルで開く（file:// だと localStorage が使えないので http で出す）
python -m http.server 8778 --bind 127.0.0.1
# → http://localhost:8778/tagm-LINEtest.html
```

- **tagm-test.html と同じポートで開かないこと。** 同一オリジンだと localStorage
  (`linesk:tagManager`) を共有し、旧版の `saveState()` が `suggestions` ごと消す。
- タグ付けは必ず **「全部上書き」** を選ぶ（穴埋めでは新方式が効かない）。
- 実行後の確認：

```js
console.log('候補:', Object.keys(state.suggestions).length, '件 / 1枚目:', Object.values(state.suggestions)[0])
copy(JSON.stringify(state.suggestions, null, 1))   // 惰性チェック用
copy(JSON.stringify(aiLastDropLog, null, 1))       // 脱落の内訳
```

### 関連メモ

- Geminiのモデル指定は `gemini-flash-latest`（`gemini-2.5-flash` は新規ユーザー不可）
- 429 が続く場合は AI Studio の請求先残高を疑う（複数プロジェクトが同一 Prepay 請求先を共有していて残高0で全停止した前例あり）
- 生成設定は `temperature: 0.2` / `thinkingConfig.thinkingBudget: 0`。上げる必要は今のところ無い（上記の実測より）
