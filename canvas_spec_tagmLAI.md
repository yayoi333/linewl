# Gemini Canvas 用 実装指示書：LINEスタンプ タグ付けアプリ（最小構成）

この文書を Gemini Canvas にそのまま渡してください。
参考実装は majin 氏の Canvas アプリ（PDF→画像変換）で、その書き方の規約に合わせること。

---

## 0. 完成条件（これができたら完成）

1. 画像を複数枚まとめてドロップ／選択できる
2. 「タグ付け」を押すと、**1回のAPI呼び出し**で全画像分のタグを生成する
3. 画像ごとに **採用9個** と **差し替え候補9個** が表示される
4. 候補をクリックすると採用側に移り、採用側の × で外せる
5. 結果を `{"01": [...], "02": [...]}` 形式のJSONでクリップボードにコピーできる
6. APIキーの入力欄が存在しない（Canvas の実行環境が供給する）
7. 公式タグリストを GitHub から取得し、語数と更新日を画面に表示する

---

## 1. Canvas 向けの実装規約（majin 方式）

参考コードから読み取った書き方。**これに従うこと。**

### 1-1. ファイル構成
- `export default function App()` 単一コンポーネント。props なし
- import は `react`（useState / useEffect / useRef）と `lucide-react` のアイコンのみ
- Tailwind のクラスを JSX に直接書く
- キーフレームなど Tailwind で書けないものは、コンポーネント末尾の `<style>{\` ... \`}</style>` に置く

### 1-2. 外部ライブラリは npm import しない
参考コードと同じく、**CDN から `<script>` を注入して `window.*` 経由で使う**。
そのためのヘルパーをそのまま流用する:

```js
const loadScript = (src) => {
  return new Promise((resolve, reject) => {
    if (document.querySelector(`script[src="${src}"]`)) { resolve(); return; }
    const script = document.createElement('script');
    script.src = src;
    script.onload = resolve;
    script.onerror = reject;
    document.body.appendChild(script);
  });
};
```

`useEffect` で読み込み、`librariesLoaded` が true になるまで操作をさせない。
**ただし今回の最小構成では外部ライブラリは不要**（ZIP も PDF も扱わない）。
将来ZIP対応を足すときだけ JSZip を上のやり方で入れる。

### 1-3. 状態の持ち方
- 全体の進行は単一の文字列 `status`: `'idle' | 'processing' | 'complete' | 'error'`
- 個別処理の状態は別に持つ（参考コードの `gifStatus` などと同じ考え方）
- 中断フラグは `useRef`（`cancelRef.current`）
- エラーは `errorMsg` に文字列で入れ、`status='error'` にして専用画面を出す

### 1-4. 見た目
参考コードのトーンに寄せる。ダーク基調 + backdrop-blur のガラス風カード + Aurora背景。
ただし**過度に凝らなくてよい**。優先は動作。

### 1-5. その他
- 全部クライアント側で完結。画像をどこかにアップロードしない
- UI文言は日本語、システム表示は英語（参考コードと同じ）
- `localStorage` は使わない（Canvas での可否が未確認のため。状態は React state のみで持つ）

---

## 2. Gemini API の呼び出し

### 2-1. APIキー
```js
const apiKey = ""; // Canvas の実行環境が供給する。ユーザーには入力させない
const url = `https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent?key=${apiKey}`;
```

> **最初に確認すること**: この方式で 200 が返るか。返らない場合は Canvas で利用できる
> 呼び出し方法に読み替えてよい。**ユーザーにAPIキーを入力させる形にはしないこと**（それだと作る意味がない）。

### 2-2. モデル
`gemini-2.5-flash` 系の画像入力対応モデルを使う。Canvas で利用可能なものを選ぶ。

### 2-3. 生成設定（そのまま使う）
```js
generationConfig: {
  responseMimeType: 'application/json',
  temperature: 0.2,
  thinkingConfig: { thinkingBudget: 0 }
}
```
`thinkingBudget: 0` は意図的。無料枠ではトークンを増やすほど混雑・制限に当たりやすい。
実測で、思考を有効にしなくてもタグの精度は十分だった。

### 2-4. リクエストの組み立て
`contents[0].parts` に、以下の順で詰める。**全画像を1リクエストにまとめる。**

```
[0]  { text: <§3のプロンプト全文> }
[1]  { text: "番号: 01" }
[2]  { inlineData: { mimeType: "image/png", data: <base64> } }
[3]  { text: "番号: 02" }
[4]  { inlineData: {...} }
...
```

**画像は長辺512pxに縮小してから base64 にすること。** canvas に描いて `toDataURL('image/png')` の
先頭 `data:image/png;base64,` を除いた部分を渡す。原寸だと無駄にトークンを食う。

---

## 3. プロンプト（一字一句そのまま使う）

`${TAG_LIST}` の部分に、§4で取得した配列を `officialTags.join('、')` で連結した文字列を差し込む。

```
あなたは「LINEスタンプ・絵文字 タグ付けメーカー」です。

タグとは何かの定義:
タグは画像の説明文ではありません。「LINEのトーク画面でユーザーが何と入力したら、このスタンプが変換候補に出てほしいか」という検索キーワードです。常に「打つ側の言葉」を考えてください。

渡された画像1枚ごとに、使用可能タグリストの中から18個を、確信度の高い順に並べて出力してください。先頭9個がそのまま採用され、残りは差し替え候補としてユーザーに提示されます。

18個の内訳の目安:
1. 直訳（2〜3個）画像内の文字をそのままタグ化する。略語・くだけた表記は展開すること（例:「おは」→おはよう /「いてら」→いってらっしゃい /「たす」→助かる /「はらへ」→空腹・ごはん /「しょぼん」→しょんぼり）
2. 言い換え（4〜6個）同じ意味・同じ場面で使う別の言葉
3. 相手が打つ言葉（2〜3個）このスタンプを受け取る側や、催促する側が入力しそうな語（例:「今は返信できません」→ 既読・未読・まだ？・おーい /「連絡します」→ 待ってる・おまたせ）
4. 絵柄から（2〜3個）画像に写っているモノ・ポーズ・動作。ただし**モノの名前をそのままタグにしないこと**。リストに載っているモノの名前は季節（春・夏・秋・冬・年末年始）と動物のカテゴリだけで、それ以外の日用品・場所・一般名詞は登録されていない。「そのモノを見た人がトーク画面で打つ言葉」に変換すること（例: 時計→もうすぐ着く / カバン→出かける・出勤 / 走っている→急ぐ・ダッシュ / マグカップ→休憩・飲む / 浮き輪→泳ぐ / ベッド→寝る・おやすみ / 夜空→こんばんは）
5. 感情・反応（2〜3個）表情から読み取れる気持ち
6. 定番（2〜3個）そのスタンプが使われる場面でよく打たれる汎用語

やってはいけないこと:
・季節・行事のタグ（夏、冬、春、秋、夏祭り、花火、年末年始 など）は、画像にその要素がはっきり描かれている場合を除いて選ばない。今の季節に合わせて入れるのは禁止。
・動物・キャラクター種別のタグ（イヌ、ネコ、クマ、ウサギ、トリ など）は、その動物だと確実に判別できる場合に1個だけ。迷ったら選ばない。複数の動物名を並べるのは禁止。
・表情の読み違いに注意する。口を大きく開けた笑顔を怒りと取り違えないこと。画像内の文字がポジティブな内容なら、表情もポジティブ側に解釈すること。
・画像内の文字が専門用語・固有名詞などで、リストに対応する語が無い場合は、無理に音や字面が近い語を当てないこと。その場合は絵柄・表情・ポーズから選ぶ。

最重要ルール:
・タグは必ず使用可能タグリストの中から完全一致で選ぶこと。リストに無い語は、意味がどれだけ近くても出力しないこと（創作タグは禁止）。逆に、リストに載っている語であれば、言い換え・略語の展開・意訳の結果として選んでよい。
・出力する前に、選んだ語を1つずつ、使用可能タグリストに一字一句そのままの形で載っているか確認すること。送り仮名・漢字とひらがなの違い・活用形の違いも、1文字でも違えば「無い」とみなして捨て、リストに載っている表記に置き換えるか、別の語を選び直すこと。
・「意味は合っているがリストに無い語」を作りがちなので特に注意すること。実際にあった間違い: 「笑顔」は無い（正しくは「にっこり」）、「お疲れ様」は無い（正しくは「おつかれさま」）、「大好き」は無い（正しくは「好き」）、「連絡する」は無い（正しくは「連絡」）、「間違い」は無い（正しくは「間違いない」）。
・1枚につき必ず18個。同じ画像内でタグを重複させないこと。
・確信度の高い順に並べること。並び順に意味がある。
・「番号: NN」というテキストの直後に続く画像が、その番号に対応する画像です。

出力形式は次のJSONのみ。説明文やコードブロック記法は一切含めないこと:
{"01": ["タグ1","タグ2", ...], "02": [...]}

使用可能タグリスト: ${TAG_LIST}
```

### プロンプト内の記述の根拠（変更しないこと）

LINE公式の「おすすめのタグ」を画面録画5本・50スタンプ分実測して割り出した構成。
数字は実測値なので、勝手に足したり削ったりすると精度が落ちる。

- **18個**という数はLINE公式の提示数と同じ（50件すべてで18個ちょうどだった）
- **枠3「相手が打つ言葉」** はLINE独自の特徴。これを入れると `ただいま` → `おかえり` `待ってた` `会いたい` のような、受け手側の語が出るようになる
- **季節タグの禁止** は、LINE側が8月に「春・冬・年末年始」を出すなど暦と無関係なノイズを混ぜていたため、意図的に真似していない
- **near-miss の5組** は実測で頻出した誤りだけを挙げている。これを入れてから該当語の脱落がほぼ消えた

---

## 4. 公式タグ一覧（GitHubから読み込む）

**コードに埋め込まないこと。** LINE公式のタグはまれに更新されるため、埋め込むと
そのたびにアプリを作り直すことになる。**起動時に取得する。**

### 4-1. 取得先

主: `https://yayoi333.github.io/linewl/tags_sticker_latest.json`
副: `https://raw.githubusercontent.com/yayoi333/linewl/main/tags_sticker_latest.json`

両方とも `Access-Control-Allow-Origin: *` を返すので、ブラウザから直接 fetch できる
（確認済み）。主が失敗したら副を試す。

### 4-2. JSONの形と、配列への変換

```json
{
  "product_type": "sticker",
  "last_updated": "2026-07-30 10:01:43",
  "total": 608,
  "categories": {
    "会話 (157)": ["はぁ", "おはよう", "ちーん", ...],
    "表現 (130)": [...],
    "感情 (61)": [...],
    "行動 (42)": [...],
    "春 (20)": [...], "夏 (45)": [...], "秋 (24)": [...], "冬 (21)": [...],
    "年末年始 (29)": [...],
    "動物 (25)": [...],
    "その他 (54)": [...]
  }
}
```

`categories` の全カテゴリの配列を、出現順に連結して1本の配列にする:

```js
const [officialTags, setOfficialTags] = useState(null); // null = 未読込
const [tagsUpdatedAt, setTagsUpdatedAt] = useState('');

useEffect(() => {
  const URLS = [
    'https://yayoi333.github.io/linewl/tags_sticker_latest.json',
    'https://raw.githubusercontent.com/yayoi333/linewl/main/tags_sticker_latest.json'
  ];
  (async () => {
    for (const url of URLS) {
      try {
        const res = await fetch(url);
        if (!res.ok) continue;
        const json = await res.json();
        const flat = [];
        for (const arr of Object.values(json.categories || {})) {
          if (Array.isArray(arr)) flat.push(...arr.filter(t => typeof t === 'string'));
        }
        if (flat.length === 0) continue;
        setOfficialTags(flat);
        setTagsUpdatedAt(json.last_updated || '');
        return;
      } catch (e) { /* 次のURLを試す */ }
    }
    setErrorMsg('公式タグリストを取得できませんでした。通信環境を確認して、リロードしてください。');
    setStatus('error');
  })();
}, []);
```

### 4-3. 取得できなかったときは動かさない

タグリストが無いと §5 の検証ができず、**AIが作った存在しないタグをそのまま通してしまう**。
これは審査で問題になり得るので、**取得失敗時はタグ付けを実行させないこと。**
埋め込みのフォールバックは持たない（古いリストで動くと、更新した意味が消える）。

### 4-4. 取得状態を画面に出す

- 読込中: 「タグリスト読込中…」と出し、タグ付けボタンを無効化する
- 成功時: 「公式タグ 608語 / 更新 2026-07-30」のように**語数と更新日を小さく表示する**
  （どの版のリストで付けたかが後から分かる）

### 4-5. カテゴリ構成（プロンプトの根拠）

| カテゴリ | 語数 | 性質 |
|---|---|---|
| 会話 / 表現 / 感情 / 行動 | 390 | ほぼ全部「打つ言葉」 |
| 春 / 夏 / 秋 / 冬 / 年末年始 | 139 | **モノの名前はここに集中**（いちご・うちわ・カボチャ等） |
| 動物 | 25 | イヌ・ネコ・クマ 等 |
| その他 | 54 | 電話・連絡・誕生日 ＋ タイ語/中国語 |

プロンプトの枠4で「モノの名前は季節と動物のカテゴリだけ」と書いているのはこの内訳が根拠。

---

## 5. 応答の検証（必須）

AIが返したタグを、そのまま信用しない。**1語ずつ §4 で取得したリストに完全一致で存在するか確認し、
無いものは捨てる。** 実測で応答の約8%がリスト外だった。

```js
const officialTagSet = new Set(officialTags); // 毎回 includes すると遅いので Set にする
const valid = [];
const dropped = [];
for (const tag of tagsFromAI) {
  if (typeof tag !== 'string') continue;
  if (!officialTagSet.has(tag)) { dropped.push(tag); continue; }
  if (valid.includes(tag)) continue;   // 同一画像内の重複を除く
  valid.push(tag);
}
// 先頭9個を採用、全件を候補として保持する
const adopted = valid.slice(0, 9);
const suggestions = valid;
```

`dropped` は画面に出さなくてよいが、`console.log` には残す。改善の手がかりになる。

---

## 6. 無料枠での動作（重要）

利用者は無料枠で使う前提。実測した制約:

| 項目 | 実測値 |
|---|---|
| リクエスト上限 | **20 / 分** |
| 混雑時 | 無料枠は優先度が下がり **HTTP 503** が出やすい |

### 6-1. リクエスト数を最小にする
**全画像を1リクエストにまとめる**（LINEのスタンプは最大40個なので1回で収まる）。
40枚を超える場合だけ40枚ずつに分割し、分割間に3秒空ける。

### 6-2. リトライ
429（呼びすぎ）と 5xx（混雑）は待てば直る。**8秒 → 20秒** と伸ばしながら、
**実行全体で最大2回まで**再試行する。チャンクごとに数えると掛け算で膨らむので必ず全体で数える。

サーバが `error.details[].retryDelay`（例 `"55s"`）を返したら、その秒数+1秒を優先する。

### 6-3. 枠切れは即中断
エラーメッセージに `quota` が含まれる場合、その分のうちは何度叩いても通らない。
**再試行せず即座に中断し、待つべき秒数を画面に出す。**

```
無料枠の上限（1分あたりのリクエスト数）に達しました。
約56秒待ってから、もう一度実行してください。
詳細: <サーバの原文>
```

### 6-4. 失敗理由は必ず画面に出す
「N枚中0枚にタグを付けました」だけだと原因が分からない。
**サーバが返した原文（`error.message`）を必ず併記する。** 診断に直結する。

---

## 7. 画面仕様（最小構成）

### 7-1. idle
- 画像のドロップ領域（PNG/JPEG、複数可）
- 選択済みのサムネイル一覧と枚数表示
- 「タグ付け」ボタン

### 7-2. processing
- 進捗表示。リトライ待ちのときは理由を出し分ける
  - 429 → 「呼び出し制限のため N秒待機中…」
  - 5xx → 「モデル混雑のため N秒待機中…」

### 7-3. complete
画像ごとに1行。

```
[サムネイル]  [大丈夫 ×] [連絡 ×] [問題ない ×] … 採用9個（実線チップ）
              ─────────────────────────────
              おすすめ (未読) (既読) (返信不要) … 候補9個（破線チップ）
```

- 採用チップの × で外す
- 候補チップのクリックで採用側に加える（**採用済みの語は候補から消す**）
- 採用数のカウンタ `9/9` を出し、9を超えたら色を変えて警告する（超えても操作は止めない）

### 7-4. 出力
「JSONをコピー」ボタン。形式:

```json
{
  "01": ["大丈夫", "連絡", "問題ない", "OK", "了解", "今", "電話", "心配", "すみません"],
  "02": [...]
}
```

**候補（採用していない9個）はJSONに含めない。** 採用した分だけを出す。

> `navigator.clipboard.writeText()` が Canvas で使えない場合は、
> textarea に出して選択状態にするフォールバックを用意すること。

---

## 8. やらないこと（最小構成の範囲外）

- ZIP の取り込み／書き出し
- メタ情報（タイトル・説明文）の編集
- PNG の切り出し・リサイズ出力
- タグの手動検索・カテゴリブラウズ
- APIキーの入力欄（**作ってはいけない**）
- localStorage への保存

動くようになってから必要に応じて足す。

---

## 9. 実測データ（参考・仕様の裏付け）

同一の16枚（会話系スタンプ）で計測した、このプロンプトの実力:

| 指標 | 値 |
|---|---|
| AIが返す個数 | 常に18個（16枚すべてで達成） |
| リスト外で捨てられる率 | 8.3%（24/288） |
| 候補どうしの重複率 | 7%（低いほど画像ごとに違う語が出ている） |
| 1枚しか持たない固有タグ | 6.4個/枚 |

装飾スタンプ（いちご・ハート等、文字なし）では重複率が50%まで上がる。
これはAIの手抜きではなく、絵から取れる情報が「かわいい」「きらきら」しかないため。
**文字入りのスタンプでは十分に機能する。**
