# Gemini Canvas 指示書：「タグ付けちゃん LINE AI風味」

この文書だけで作れるように書いてある。**まっさらな Canvas セッションにそのまま貼ってよい。**

---

## 1. 作るもの

LINEクリエイターズマーケットに申請するスタンプ・絵文字の**タグ付けリスト（JSON）**を作るツール。

画像を渡すと、LINE公式のタグ一覧の中から各コマに合うタグを選び、
`{"01": [...], "02": [...]}` の形で出力する。それをLINEの一括タグ付けツールに貼り付けて使う。

**アプリ名: 「タグ付けちゃん LINE AI風味」**

### 完成条件

1. 画像をドラッグ＆ドロップ／選択できる（複数枚可）
2. **スタンプ / スタンプ以外** をボタンで選べ、使うタグリストが変わる
3. 「タグ付け」で **1回のAPI呼び出し**、コマごとに **18個** の候補が返る
4. コマごとに **採用9個** と **差し替え候補9個** が表示される
5. 候補をクリックすると採用に移り、採用側の × で外せる
6. 結果をクリップボードにコピー／JSONファイルとして保存できる
7. **APIキーの入力欄が存在しない**
8. ダークモード / ライトモードを切り替えられる
9. ヘッダーにタグリストの更新日を出す
10. 最下部に `Developed by yayoi 2026`

---

## 2. 実装規約

- `export default function App()` の単一コンポーネント。props なし
- import は `react`（useState / useEffect / useMemo / useRef）と `lucide-react` のアイコンのみ
- Tailwind のクラスを JSX に直接書く。Tailwind で書けないものは末尾の `<style>` ブロックに置く
- **外部ライブラリは npm import しない。** 必要になったら CDN から `<script>` を注入して
  `window.*` 経由で使う（今回の範囲では不要）
- 進行状態は文字列1本で持つ（`'idle' | 'processing' | 'complete' | 'error'`）
- 全部クライアント側で完結。画像をどこかへアップロードしない
- UI文言は日本語
- **`localStorage` は使わない**（Canvas での可否が不明。状態は React state のみ）

---

## 3. 画面仕様

### 3-1. 全体構成

```
┌──────────────────────────────────────────────┐
│ [🏷] タグ付けちゃん LINE AI風味          [☀/🌙] │
│  LINEスタンプ・絵文字の画像から公式対応タグリストJSONを自動生成 │
│                  タグリスト最終更新日: 2026-07-30 10:01:43 │
└──────────────────────────────────────────────┘

┌── 左：設定と入力 ──┐  ┌── 右：結果 ──────────┐
│ 作品種別の選択      │  │ 生成結果・タグ付けリスト │
│ [スタンプ][スタンプ以外]│ │                      │
│                    │  │  （コマごとのカード）    │
│ 画像のアップロード   │  │                      │
│  ドロップゾーン      │  │                      │
│  サムネイル一覧      │  │                      │
│  [タグ付けリストを作る]│ │  [コピー][JSON保存]    │
└────────────────────┘  └──────────────────────┘

                Developed by yayoi 2026
```

### 3-2. ヘッダー

- タイトル: **タグ付けちゃん LINE AI風味**
- サブタイトル: 「LINEスタンプ・絵文字の画像から公式対応タグリストJSONを自動生成」
- 右側に `タグリスト最終更新日: 2026-07-30 10:01:43`
  **語数（608語など）は出さない。日付だけ。**
- ダーク/ライト切替ボタン（右上固定でよい）。**初期値はダーク**

### 3-3. 作品種別の選択

「スタンプ」「スタンプ以外」の2択ボタン。初期値は「スタンプ」。
**ユーザーが必ず自分で選ぶ。AIに判定させない。**

「スタンプ以外」を選んだときだけ、補足を小さく出す:
> 絵文字、アニメーションスタンプ・絵文字はこちら

### 3-4. 結果のカード（コマごと）

```
┌─ 01 ──────────────────────────── 9/9 ─┐
│ 採用タグ                                │
│ [了解 ×] [承知 ×] [OK ×] … 9個          │
│ ────────────────────────────────────   │
│ 差し替え候補（クリックで追加）             │
│ (+ わかった) (+ 大丈夫) (+ 仕事中) …      │
└─────────────────────────────────────────┘
```

- 採用チップ = 実線。× で外せる
- 候補チップ = 破線。クリックで採用側へ移る。**採用済みの語は候補から消す**
- カウンタ `9/9`。9を超えたら色を変えて警告する（超えても操作は止めない）

### 3-5. 出力

- 「タグ付けリストをコピー」ボタン
- 「JSONファイルを保存」ボタン
- **含めるのは採用分だけ。候補は含めない**

```json
{
  "01": ["了解", "承知", "OK", "連絡", "はーい", "YES", "承認", "問題ない", "任せろ"],
  "02": ["..."]
}
```

コピーの下に案内を出す:
> このリストをコピーして、一括タグ付けツールに貼り付け（ペースト）してください

---

## 4. タグリストの取得

**コードに埋め込まない。** LINE公式のタグはまれに更新されるので、起動時に取得する。

```js
const OFFICIAL_TAGS_URL    = 'https://yayoi333.github.io/linewl/tags_sticker_latest.json';
const AUTOSUGGEST_TAGS_URL = 'https://yayoi333.github.io/linewl/tags_sticker_autosuggest_only.json';
```

両方とも `Access-Control-Allow-Origin: *` を返すのでブラウザから直接 fetch できる（確認済み）。

### 4-1. JSONの形

```json
// tags_sticker_latest.json
{ "last_updated": "2026-07-30 10:01:43", "total": 608,
  "tags": ["100点", "2026", "NG", "..."],
  "categories": { "会話 (157)": ["..."] } }
```

`tags` が608語のフラット配列。**これを使う。**
`categories` は同じ内容を分類したものなので使わなくてよい。

```json
// tags_sticker_autosuggest_only.json
{ "tags": [ {"tag": "むぅ", "category": "感情"}, {"tag": "休み", "category": "状況・フレーズ"} ] }
```

オートサジェスト側は**オブジェクトの配列**なので `.map(item => item.tag)` で取り出す。

### 4-2. 種別によるプールの切り替え

```js
const availableTags = itemType === 'emoji'
  ? officialTags
  : [...new Set([...officialTags, ...autosuggestTags])];
const availableTagsSet = new Set(availableTags);
```

**スタンプとスタンプ以外の差は、オートサジェスト専用の2語（`むぅ` `休み`）を足すかどうかだけ。**
絵文字専用のタグリストは存在しない。タグ上限はどちらも9個。

### 4-3. 取得できなかったら動かさない

タグリストが無いと §7 の検証ができず、**AIが作った存在しないタグをそのまま通してしまう**。
審査で問題になり得るので、**取得失敗時はタグ付けを実行させないこと。**
埋め込みのフォールバックは持たない（古いリストで動くと更新した意味が消える）。

読込中は「タグリストを最新状態に更新中...」と出し、ボタンを無効化する。
失敗したら理由と「再読み込み」ボタンを出す。

---

## 5. Gemini API の呼び出し

### 5-1. APIキー

```js
const apiKey = ""; // Canvas の実行環境が供給する。ユーザーには入力させない
const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent?key=${apiKey}`;
```

**ユーザーにAPIキーを入力させる形にしてはいけない。** それだと Canvas で作る意味がなくなる。

### 5-2. モデル

画像入力に対応した Gemini Flash 系。Canvas で利用可能なものを選ぶ。

### 5-3. リクエスト

**全画像を1リクエストにまとめる。** 画像パーツの直前にファイル名を添える。

```js
const parts = [];
for (const f of uploadedFiles) {
  parts.push({ text: `ファイル名: ${f.name}` });
  parts.push({ inlineData: { mimeType: f.mimeType, data: f.base64 } });
}
parts.push({ text: promptText });

const payload = {
  contents: [{ role: 'user', parts }],
  generationConfig: {
    responseMimeType: 'application/json',
    temperature: 0.2,
    thinkingConfig: { thinkingBudget: 0 }
  }
};
```

**画像はリサイズしないこと。** 一覧画像を縮小すると1コマの解像度が落ち、
スタンプ内の文字が読めなくなる。文字が読めないと §6 の枠1（直訳）が機能しない。

`thinkingBudget: 0` は意図的。無料枠ではトークンを増やすほど混雑・制限に当たりやすい。
実測で、思考を有効にしなくても精度は十分だった（むしろ有効にすると完走できなかった）。

### 5-4. 応答の取り出し

`candidates[0].content.parts[0].text` を取り、JSONとして解析する。
`responseMimeType` を指定していてもコードブロックで囲まれてくることがあるので、
**囲みを剥がすフォールバックを必ず入れる。**

```js
const extractAndParseJSON = (text) => {
  if (!text) throw new Error('レスポンスが空です。');
  let t = text.trim();
  if (t.startsWith('```')) {
    t = t.replace(/^```(?:json)?\s*/i, '').replace(/\s*```$/i, '').trim();
  }
  try {
    return JSON.parse(t);
  } catch (e) {
    const m = t.match(/\{[\s\S]*\}/);
    if (m) return JSON.parse(m[0]);
    throw new Error('AIからの応答をJSONとして解析できませんでした。');
  }
};
```

キーは `"1"` で返ることがあるので `String(parseInt(k, 10)).padStart(2, '0')` で正規化する。

---

## 6. プロンプト（一字一句そのまま使う）

`${TAG_LIST}` に `availableTags.join('、')` を差し込む。

```
あなたは「LINEスタンプ・絵文字 タグ付けメーカー」です。

タグとは何かの定義:
タグは画像の説明文ではありません。「LINEのトーク画面でユーザーが何と入力したら、このスタンプが変換候補に出てほしいか」という検索キーワードです。常に「打つ側の言葉」を考えてください。

渡される画像は、次のどちらかです。両方に対応してください。
・一覧画像: スタンプまたは絵文字が並んだ画像。1枚に複数のコマが含まれます。
・個別画像: 1枚に1コマだけの画像。

番号の決め方は、次の優先順で判断してください。
1. コマの近くに「01」「02」のような番号が印字されていれば、その番号をJSONのキーにする
2. 印字が無ければ、直前に添えられた「ファイル名: ...」に含まれる番号を使う
3. どちらも無ければ、一覧画像なら左上から右へ・行が変わったら次の行へ、個別画像なら渡された順に、「01」から連番を振る

「main」「tab」「stab」「key」など、番号ではない見出しが付いたコマは、タグ付けの対象外として無視してください。

コマ1つごとに、使用可能タグリストの中から18個を、確信度の高い順に並べて出力してください。先頭9個がそのまま採用され、残りは差し替え候補としてユーザーに提示されます。

18個の内訳の目安:
1. 直訳（2〜3個）コマ内の文字をそのままタグ化する。略語・くだけた表記は展開すること（例:「おは」→おはよう /「いてら」→いってらっしゃい /「たす」→助かる /「はらへ」→空腹・ごはん /「しょぼん」→しょんぼり）
2. 言い換え（4〜6個）同じ意味・同じ場面で使う別の言葉
3. 相手が打つ言葉（2〜3個）このスタンプを受け取る側や、催促する側が入力しそうな語（例:「今は返信できません」→ 既読・未読・まだ？・おーい /「連絡します」→ 待ってる・おまたせ）
4. 絵柄から（2〜3個）コマに描かれているモノ・ポーズ・動作。ただし**モノの名前をそのままタグにしないこと**。リストに載っているモノの名前は季節（春・夏・秋・冬・年末年始）と動物のカテゴリだけで、それ以外の日用品・場所・一般名詞は登録されていない。「そのモノを見た人がトーク画面で打つ言葉」に変換すること（例: 時計→もうすぐ着く / カバン→出かける・出勤 / 走っている→急ぐ・ダッシュ / マグカップ→休憩・飲む / 浮き輪→泳ぐ / ベッド→寝る・おやすみ / 夜空→こんばんは）
5. 感情・反応（2〜3個）表情から読み取れる気持ち
6. 定番（2〜3個）そのスタンプが使われる場面でよく打たれる汎用語

やってはいけないこと:
・季節・行事のタグ（夏、冬、春、秋、夏祭り、花火、年末年始 など）は、コマにその要素がはっきり描かれている場合を除いて選ばない。今の季節に合わせて入れるのは禁止。
・動物・キャラクター種別のタグ（イヌ、ネコ、クマ、ウサギ、トリ など）は、その動物だと確実に判別できる場合に1個だけ。迷ったら選ばない。複数の動物名を並べるのは禁止。
・表情の読み違いに注意する。口を大きく開けた笑顔を怒りと取り違えないこと。コマ内の文字がポジティブな内容なら、表情もポジティブ側に解釈すること。
・コマ内の文字が専門用語・固有名詞などで、リストに対応する語が無い場合は、無理に音や字面が近い語を当てないこと。その場合は絵柄・表情・ポーズから選ぶ。
・隣のコマの内容を混ぜないこと。1コマずつ独立して見ること。

最重要ルール:
・タグは必ず使用可能タグリストの中から完全一致で選ぶこと。リストに無い語は、意味がどれだけ近くても出力しないこと（創作タグは禁止）。逆に、リストに載っている語であれば、言い換え・略語の展開・意訳の結果として選んでよい。
・出力する前に、選んだ語を1つずつ、使用可能タグリストに一字一句そのままの形で載っているか確認すること。次のどれか1つでも違えば「リストに無い」とみなして捨て、リストに載っている表記に置き換えるか、別の語を選び直すこと。
　- 漢字・ひらがな・カタカナの違い
　- 全角・半角の違い
　- 大文字・小文字の違い
　- 送り仮名の違い
　- 記号やスペースの有無
　- 活用形の違い
・「意味は合っているがリストに無い語」を作りがちなので特に注意すること。実際にあった間違い: 「笑顔」は無い（正しくは「にっこり」）、「お疲れ様」は無い（正しくは「おつかれさま」）、「大好き」は無い（正しくは「好き」）、「連絡する」は無い（正しくは「連絡」）、「間違い」は無い（正しくは「間違いない」）。
・1コマにつき必ず18個。同じコマ内でタグを重複させないこと。
・確信度の高い順に並べること。並び順に意味がある。
・値は必ず文字列の配列にすること。エラーオブジェクトや説明文を入れてはいけません。
　禁止する形: "02": {"error": "18個の許可タグを確認できません", "confirmed_tags": ["おつかれさま"]}
　正しい形:   "02": ["タグ1", "タグ2", ... , "タグ18"]
　18個そろわない場合でも、必ず文字列の配列で、選べただけを返すこと。

出力形式は次のJSONのみ。説明文やコードブロック記法は一切含めないこと:
{"01": ["タグ1","タグ2", ...], "02": [...]}

使用可能タグリスト: ${TAG_LIST}
```

### 6-1. このプロンプトの根拠（勝手に変えないこと）

LINE公式の「おすすめのタグ」を画面録画5本・50スタンプ分実測して割り出した構成。

- **18個**という数はLINE公式の提示数と同じ（実測50件すべてで18個ちょうどだった）
- **枠3「相手が打つ言葉」** がLINE独自の特徴。これを入れると `ただいま` → `おかえり` `待ってた`
  `会いたい` のように受け手側の語が出るようになる
- **季節タグの禁止** は、LINE側が8月に「春・冬・年末年始」を出すなど暦と無関係なノイズを
  混ぜていたため、意図的に真似していない
- **near-miss の5組** は実測で頻出した誤りだけを挙げている。入れてから該当語の脱落がほぼ消えた
- **エラーオブジェクト禁止** は、実際にAIが `{"error": ...}` を返してきた事故への対処

---

## 7. 応答の検証（必須）

AIが返したタグをそのまま信用しない。**1語ずつ照合して、無いものは捨てる。**
実測で応答の約8%がリスト外だった。

```js
// AIがエラーオブジェクトを返すことがあるので、配列でなければ空にする
const tagsFromAI = Array.isArray(raw[numKey]) ? raw[numKey] : [];

const valid = [];
const dropped = [];
for (const tag of tagsFromAI) {
  const t = String(tag).trim();
  if (!availableTagsSet.has(t)) { dropped.push(t); continue; }
  if (valid.includes(t)) continue;
  valid.push(t);
}
const adopted     = valid.slice(0, 9);
const suggestions = valid;

if (dropped.length) console.log(`[${numKey}] リスト外で除外:`, dropped);
```

**捨てたタグは画面に出さない。** ユーザーが気にする必要のない情報で、
出すと「未登録タグを置き換えますか？」のような余計な判断を強いることになる。
`console.log` にだけ残す（精度改善の手がかりになる）。

> 18個あるので、1〜2個落ちても採用9個は問題なく埋まる。
> 代替タグを探しに行く2回目のAPI呼び出しは不要。

---

## 8. 無料枠での動作（重要）

利用者は無料枠で使う前提。**「課金すれば解決」は採用しない。** 実測した制約:

| 項目 | 実測値 |
|---|---|
| リクエスト上限 | **20 / 分** |
| 混雑時 | 無料枠は優先度が下がり **HTTP 503** が出やすい |

### 8-1. リクエストは1回にまとめる
分割しない。画像が何枚でも1リクエストに入れる。

### 8-2. リトライは実行全体で2回まで

429（呼びすぎ）と 5xx（混雑）は待てば直るので再試行する。ただし**呼び出しごとに数えず、
実行全体で最大2回**にする。回数を数え間違えると無料枠をすぐ使い切る。

待ち時間は **8秒 → 20秒**。サーバが `error.details[].retryDelay`（例 `"55s"`）を
返したらその秒数+1秒を優先する。

### 8-3. 枠切れは即中断

エラーメッセージに `quota` が含まれる場合、その分のうちは何度叩いても通らない。
**再試行せず即座に中断し、待つべき秒数を画面に出す。**

```
無料枠の上限（1分あたりのリクエスト数）に達しました。
約56秒待ってから、もう一度実行してください。
詳細: <サーバが返した原文>
```

### 8-4. 失敗理由は必ず原文を併記する

「タグを付けられませんでした」だけでは原因が分からない。
**サーバが返した `error.message` を必ず画面に出す。** 診断に直結する。

待機中は理由を出し分ける:
- 429 → 「呼び出し制限のため N秒待機中…」
- 5xx → 「モデル混雑のため N秒待機中…」

---

## 9. クリップボード

Canvas は iframe 内で動くため `navigator.clipboard` が拒否されることがある。
**必ずフォールバックを用意する。**

1. `navigator.clipboard.writeText()` を試す
2. 失敗したら、画面外の `textarea` に入れて `document.execCommand('copy')`
3. それも失敗したら、**JSONを textarea に表示して手動コピーさせる**

---

## 10. やらないこと

- ZIP の取り込み／書き出し
- メタ情報（タイトル・説明文）の編集
- PNG の切り出し・リサイズ出力
- タグの手動検索・カテゴリブラウズ
- **APIキーの入力欄（作ってはいけない）**
- `localStorage` への保存
- 一覧画像のクライアント側での切り分け（番号が印字されているので不要）
- 未登録タグの表示、および置き換え機能（§7 の通り黙って捨てる）
- 作品種別の自動判定（ユーザーがボタンで選ぶ）

---

## 11. 実測データ（仕様の裏付け）

個別PNGを渡した場合の、このプロンプトの実力（会話系スタンプ16枚）:

| 指標 | 旧プロンプト | このプロンプト |
|---|---|---|
| リスト外で捨てられる率 | 11.5% | **8.3%** |
| 候補どうしの重複率 | 13% | **7%** |
| 1枚しか持たない固有タグ | 5.6個/枚 | **6.4個/枚** |
| 全枚数に出る汎用タグ | にっこり 75% | にっこり 50% |

装飾スタンプ（いちご・ハート等、文字なし）では重複率が50%まで上がる。
これはAIの手抜きではなく、絵から取れる情報が「かわいい」「きらきら」しかないため。
**文字入りのスタンプでは十分に機能する。**

> **未検証**: 上記は**個別PNG**を1枚ずつ渡した場合の数字。
> **一覧画像から読ませた場合の精度はまだ測っていない。**
> 1コマあたりの解像度が下がるぶん、枠1（直訳）が弱くなる可能性がある。
> 最初の1セットは、コンソールに出る「リスト外で除外」の量を確認すること。
