# Gemini Canvas 用 実装指示書：LINEスタンプ タグ付けメーカー

既存の Canvas 版（GPT からの移行版）に、**LINE公式AIの傾向を取り込んだタグ付けロジック**を載せる。
土台のコードはそのまま活かし、**§3 のプロンプトと §5 の候補18個方式**に差し替えるのが主眼。

参考にする書き方: majin 氏の Canvas アプリ（単一 App / CDN+window / status文字列で状態管理 /
末尾 `<style>` で keyframes）。

---

## 0. 完成条件

1. **一覧画像**（スタンプ/絵文字が並んだスクショ）を複数枚ドロップできる
2. **スタンプ / スタンプ以外** を切り替えられ、使うタグリストが変わる
3. 「タグ付け」で **1回のAPI呼び出し**、コマごとに **18個** の候補が返る
4. コマごとに **採用9個** と **差し替え候補9個** が出る
5. 候補クリックで採用に移り、採用側の × で外せる
6. `{"01": [...], "02": [...]}` をコピー／JSON保存できる
7. **APIキーの入力欄が存在しない**
8. タグリストの語数と更新日を画面に出す

---

## 1. 既存コードから変えない部分

移行版で既に動いているものは触らない。

- `OFFICIAL_TAGS_URL` / `AUTOSUGGEST_TAGS_URL` からの取得と、`itemType` によるプール切替
- `extractAndParseJSON()`（マークダウン囲みの除去とフォールバック）
- `fetchWithRetry()`（429/5xx の指数バックオフ）
- `copyToClipboard()` の textarea フォールバック
- `downloadJSON()` / `buildJson()` / `sortedEntries()`
- ダーク/ライト切替、ドロップゾーン、プレビュー、リセット

---

## 2. 入力：一覧画像（ここが最大の変更点）

### 2-1. 何を渡すか
LINE Creators Market の**スタンプ画像一覧のスクショ**。1枚に複数コマが含まれる。
**各コマの上に `01` `02` … の番号が印字されている**ので、AIはそれを読んでキーにできる。
（`main` `tab` などタグ付け対象外のコマも写り込むが、番号が無いので無視させる）

### 2-2. 画像の渡し方
既存コードのまま、`inlineData` に base64 で並べる。**縮小しないこと。**

> 個別PNGのときは長辺512pxに縮めていたが、**一覧画像では縮めてはいけない**。
> 1コマあたりの解像度が落ちて、スタンプ内の文字が読めなくなる。文字が読めないと
> §3 の枠1（直訳）が機能せず、精度が大きく落ちる。

### 2-3. パーツの順序
```
parts: [ ...画像すべて, { text: <§3のプロンプト全文> } ]
```
既存コードと同じ（画像が先、プロンプトが後）。

---

## 3. プロンプト（これに丸ごと差し替える）

`${TAG_LIST}` に `availableTags.join('、')` を差し込む。

```
あなたは「LINEスタンプ・絵文字 タグ付けメーカー」です。

タグとは何かの定義:
タグは画像の説明文ではありません。「LINEのトーク画面でユーザーが何と入力したら、このスタンプが変換候補に出てほしいか」という検索キーワードです。常に「打つ側の言葉」を考えてください。

渡された画像は、スタンプまたは絵文字の一覧です。1枚に複数のコマが並んでいます。
各コマの近くに「01」「02」のような番号が書かれています。その番号をそのままJSONのキーにしてください。
番号が読み取れないコマや、「main」「tab」など番号でない見出しが付いたコマは、タグ付けの対象外として無視してください。
番号がどこにも書かれていない場合に限り、左上から右へ、行が変わったら次の行へ、と「01」から順に振ってください。

コマ1つごとに、使用可能タグリストの中から18個を、確信度の高い順に並べて出力してください。先頭9個がそのまま採用され、残りは差し替え候補としてユーザーに提示されます。

18個の内訳の目安:
1. 直訳（2〜3個）コマ内の文字をそのままタグ化する。略語・くだけた表記は展開すること（例:「おは」→おはよう /「いてら」→いってらっしゃい /「たす」→助かる /「はらへ」→空腹・ごはん /「しょぼん」→しょんぼり）
2. 言い換え（4〜6個）同じ意味・同じ場面で使う別の言葉
3. 相手が打つ言葉（2〜3個）このスタンプを受け取る側や、催促する側が入力しそうな語（例:「今は返信できません」→ 既読・未読・まだ？・おーい /「連絡します」→ 待ってる・おまたせ）
4. 絵柄から（2〜3個）コマに描かれているモノ・ポーズ・動作。ただし**モノの名前をそのままタグにしないこと**。リストに載っているモノの名前は季節（春・夏・秋・冬・年末年始）と動物のカテゴリだけで、それ以外の日用品・場所・一般名詞は登録されていない。「そのモノを見た人がトーク画面で打つ言葉」に変換すること（例: 時計→もうすぐ着く / カバン→出かける・出勤 / 走っている→急ぐ・ダッシュ / マグカップ→休憩・飲む / 浮き輪→泳ぐ / ベッド→寝る・おやすみ / 夜空→こんばんは）
5. 感情・反応（2〜3個）表情から読み取れる気持ち
6. 定番（2〜3個）そのスタンプが使われる場面でよく打たれる汎用語

やってはいけないこと:
・季節・行事のタグ（夏、冬、春、秋、夏祭り、花火、年末年始 など）は、コマにその要素がはっきり描かれている場合を除いて選ばない。今の季節に合わせて入れるのは禁止。
・動物・キャラクター種別のタグ（イヌ、ネコ、クマ、ウサギ、トリ など）は、その動物だと確実に判別できる場合に1個だけ。迷ったら選ばない。複数の動物名を並べるのは禁止。
・表情の読み違いに注意する。口を大きく開けた笑顔を怒りと取り違えないこと。コマ内の文字がポジティブな内容なら、表情もポジティブ側に解釈すること。
・コマ内の文字が専門用語・固有名詞などで、リストに対応する語が無い場合は、無理に音や字面が近い語を当てないこと。その場合は絵柄・表情・ポーズから選ぶ。
・隣のコマの内容を混ぜないこと。1コマずつ独立して見ること。

最重要ルール:
・タグは必ず使用可能タグリストの中から完全一致で選ぶこと。リストに無い語は、意味がどれだけ近くても出力しないこと（創作タグは禁止）。逆に、リストに載っている語であれば、言い換え・略語の展開・意訳の結果として選んでよい。
・出力する前に、選んだ語を1つずつ、使用可能タグリストに一字一句そのままの形で載っているか確認すること。送り仮名・漢字とひらがなの違い・活用形の違いも、1文字でも違えば「無い」とみなして捨て、リストに載っている表記に置き換えるか、別の語を選び直すこと。
・「意味は合っているがリストに無い語」を作りがちなので特に注意すること。実際にあった間違い: 「笑顔」は無い（正しくは「にっこり」）、「お疲れ様」は無い（正しくは「おつかれさま」）、「大好き」は無い（正しくは「好き」）、「連絡する」は無い（正しくは「連絡」）、「間違い」は無い（正しくは「間違いない」）。
・1コマにつき必ず18個。同じコマ内でタグを重複させないこと。
・確信度の高い順に並べること。並び順に意味がある。

出力形式は次のJSONのみ。説明文やコードブロック記法は一切含めないこと:
{"01": ["タグ1","タグ2", ...], "02": [...]}

使用可能タグリスト: ${TAG_LIST}
```

### 3-1. 旧プロンプトから変わった点と、その理由

移行版に入っている旧プロンプト（GPT時代のもの）は**置き換える**。主な違い:

| | 旧（GPT時代） | 新 |
|---|---|---|
| タグの定義 | 「選定の観点: キャラクター/表情/感情/動作…」＝**絵の説明** | 「トーク画面で打つ検索キーワード」＝**打つ側の言葉** |
| 個数 | 9個 | **18個**（上位9個を採用、残りを差し替え候補に） |
| 言い換え | 「言い換え・翻訳からタグを作らない」と禁止 | **明示的に許可**（リストにある語なら意訳してよい） |
| 枠 | なし | **7枠**の内訳を指定（うちノイズ枠は不採用） |

**「相手が打つ言葉」の枠がLINE独自の特徴**で、これを入れると `ただいま` → `おかえり` `待ってた` `会いたい`
のように受け手側の語が出るようになる。旧プロンプトでは出ない。

数字は画面録画5本・50スタンプの実測に基づく。**勝手に足したり削ったりすると精度が落ちる。**

---

## 4. タグリスト（既存コードのまま。1点だけ確認）

既存の取得処理をそのまま使う。`officialJson.tags` は608語のフラット配列なので変換は不要
（`categories` をフラット化しても同じ内容になる。どちらでもよい）。

```js
const availableTags = itemType === 'emoji'
  ? tagData.officialTags                                   // 608語
  : [...new Set([...officialTags, ...autosuggestTags])];   // 610語
```

**スタンプとスタンプ以外の差は、オートサジェスト専用タグ（`むぅ` `休み`）を足すかどうかだけ。**
絵文字専用のタグリストは存在しない。

> **未確認**: 絵文字のタグ上限が9個かどうかは実測していない。スタンプは9個。
> 違っていたら `itemType` で採用数を変えること。

---

## 5. 応答の検証と、18個の使い方

### 5-1. 検証
コマごとに、返ってきた18個を1つずつ `availableTagsSet` で照合する。

```js
const valid = [];
const dropped = [];
for (const tag of tagsFromAI) {
  const t = String(tag).trim();
  if (!availableTagsSet.has(t)) { dropped.push(t); continue; }
  if (valid.includes(t)) continue;
  valid.push(t);
}
const adopted     = valid.slice(0, 9);  // 自動採用
const suggestions = valid;              // 差し替え候補（採用済みは表示時に除く）
```

### 5-2. 「未登録タグの置き換え」機能は削除してよい

移行版にある `replaceUnregisteredTags()`（2回目のAPI呼び出しで代替タグを探す）は、
**18個方式では不要になる。**

理由: 実測でリスト外は約8%。18個中1〜2個が落ちても **16個以上残る**ので、
採用9個は問題なく埋まる。落ちた分は候補が減るだけ。

削除すると**APIリクエストが1回減る**。無料枠（20回/分）では効いてくる。

代わりに、落ちたタグは `console.log` に出しておく（改善の手がかりになる）。
画面には出さなくてよい。

---

## 6. 無料枠での動作

利用者は無料枠前提。実測した制約:

| 項目 | 実測値 |
|---|---|
| リクエスト上限 | **20 / 分** |
| 混雑時 | 無料枠は優先度が下がり **HTTP 503** が出やすい |

### 6-1. リクエストは1回にまとめる
一覧画像は数枚なので、**全部を1リクエストに入れる**。分割しない。

### 6-2. リトライは絞る
既存の `fetchWithRetry` を、**実行全体で最大2回**に変更する。
（現状は `maxRetries = 3` で呼び出しごとに数えるため、枠を食いやすい）

サーバが `error.details[].retryDelay`（例 `"55s"`）を返したらその秒数+1秒を優先する。

### 6-3. 枠切れは即中断
エラーに `quota` が含まれる場合、その分のうちは何度叩いても通らない。
**再試行せず中断し、待つべき秒数を画面に出す。**

```
無料枠の上限（1分あたりのリクエスト数）に達しました。
約56秒待ってから、もう一度実行してください。
詳細: <サーバの原文>
```

### 6-4. 失敗理由は必ず原文を併記する
「0枚にタグを付けました」だけでは原因が分からない。`error.message` を必ず画面に出す。

### 6-5. 生成設定
```js
generationConfig: {
  responseMimeType: 'application/json',
  temperature: 0.2,
  thinkingConfig: { thinkingBudget: 0 }
}
```
`thinkingBudget: 0` は意図的。無料枠ではトークンを増やすほど混雑・制限に当たりやすい。
実測で、思考を有効にしなくても精度は十分だった（むしろ有効にすると完走できなかった）。

---

## 7. 画面仕様（変更箇所のみ）

結果表示を、**採用と候補の2段**にする。既存の1段（9個をチップで並べるだけ）から変更。

```
┌─ 01 ─────────────────────────── 9/9 ─┐
│ 採用タグ                              │
│ [了解 ×] [承知 ×] [OK ×] … 9個        │
│ ─────────────────────────────────    │
│ 差し替え候補（クリックで追加）          │
│ (+ わかった) (+ 大丈夫) (+ 仕事中) …   │
└───────────────────────────────────────┘
```

- 採用チップ = 実線、× で外せる
- 候補チップ = 破線、クリックで採用側へ移る。**採用済みの語は候補から消す**
- カウンタ `9/9`。9を超えたら色を変えて警告（超えても操作は止めない）
- コピー／JSON保存に含めるのは**採用分だけ**。候補は含めない

既存の「未登録タグがあります」警告ボックスは、§5-2 の通り不要になるので削除してよい。

---

## 8. やらないこと

- ZIP の取り込み／書き出し
- メタ情報（タイトル・説明文）の編集
- APIキーの入力欄（**作ってはいけない**）
- localStorage への保存
- 一覧画像のクライアント側での切り分け（**番号が印字されているので不要**）

---

## 9. 実測データ（仕様の裏付け）

個別PNGを渡した場合の、このプロンプトの実力（会話系16枚）:

| 指標 | 旧プロンプト | 新プロンプト |
|---|---|---|
| リスト外で捨てられる率 | 11.5% | **8.3%** |
| 候補どうしの重複率 | 13% | **7%** |
| 1枚しか持たない固有タグ | 5.6個/枚 | **6.4個/枚** |
| 全枚数に出る汎用タグ | にっこり 75% | にっこり 50% |

装飾スタンプ（いちご・ハート等、文字なし）では重複率が50%まで上がる。
これはAIの手抜きではなく、絵から取れる情報が「かわいい」「きらきら」しかないため。
**文字入りのスタンプでは十分に機能する。**

> **未検証**: 上記は**個別PNG**を1枚ずつ渡した場合の数字。**一覧画像から読ませた場合の精度は
> まだ測っていない。** 1コマあたりの解像度が下がるぶん、枠1（直訳）が弱くなる可能性がある。
> 最初の1セットは、個別PNG版（tagmLAI.html）の結果と見比べて確認することを勧める。
